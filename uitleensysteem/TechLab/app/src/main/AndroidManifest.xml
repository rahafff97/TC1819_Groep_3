<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.borrow">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Items"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".addItem"></activity>
        <activity android:name=".view.ViewItemContents">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Geschiedenis"
                  android:label="Geshiedenis"/>
        <activity android:name=".view.ViewBorrowedItem" />
        <activity android:name=".borrowItemContents" />
    </application>

</manifest>